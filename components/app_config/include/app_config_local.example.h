#pragma once

// Copy this file to app_config_local.h and edit local-only settings there.
// app_config_local.h is gitignored so Wi-Fi credentials stay out of commits.

#define APP_WIFI_SSID "YOUR_WIFI_SSID"
#define APP_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
