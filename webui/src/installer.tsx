// Installer placeholder; replaced in Commit C.
import { render } from "preact";
const root = document.getElementById("app");
if (root) render(<div>Installer placeholder — replaced in Commit C.</div>, root);
